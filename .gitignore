# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Local scratch / one-off probes — not part of the pipeline
*.local
_probe*.py
_candidates.json
_cache_*.txt
_tamper.py

# Local agent tooling state. Never part of the published site or the dataset.
.workbuddy-ai/

# Wrangler's local deploy cache — machine state, never part of the site
.wrangler/

# OS noise
.DS_Store
Thumbs.db
desktop.ini
_live/
